"use client";

import Header from "@/(components)/header/Header";
import Index from "@/(components)/earth";

const Game = () => {
    return (
        <main>
            <Header />
            <Index />
        </main>
    );
};

export default Game;
