"use client";

import React, { useState } from "react";
import Index from "@/(components)/earth";

const Home = () => {
    return (
        <main>
            {/* <GameController /> */}
            <Index />
        </main>
    );
};

export default Home;
