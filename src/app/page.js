"use client";

import Index from "@/(components)/earth";
import Header from "@/(components)/header/Header";

const Home = () => {
    return (
        <main>
            <Header />
            <Index />
        </main>
    );
};

export default Home;
