/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    output: "export",
    assetPrefix: "/GAME-Find-Out-Where-On-Earth",
    basePath: "/GAME-Find-Out-Where-On-Earth",
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
